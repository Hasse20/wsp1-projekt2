<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Training Program</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
            background-color: #f9f9f9;
        }
        h1 {
            color: #333;
        }
        ul {
            list-style: none;
            padding: 0;
        }
        li {
            margin: 0.5rem 0;
        }
        button {
            padding: 0.5rem 1rem;
            background-color: #28a745;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <h1>Your Training Program</h1>

    <ul>
        <li>User ID: <%= @lift["id"] %></li>
        <li>Deadlift: <%= @lift["deadlift"] %> kg</li>
        <li>Shoulder Press: <%= @lift["shoulderpress"] %> kg</li>
        <li>Bench Press: <%= @lift["benchpress"] %> kg</li>
    </ul>

    <form action="/lifts/<%= @lift['id'] %>/update" method="post">
        <button type="submit">Add 2.5kg</button>
    </form>
</body>
</html>
